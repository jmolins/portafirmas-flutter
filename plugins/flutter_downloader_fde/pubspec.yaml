name: flutter_downloader_fde
description: Temporary desktop implementations of flutter_downloader plugin
version: 0.0.1
homepage: https://github.com/jmolins

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  plugin:
    platforms:
      windows:
        pluginClass: FlutterDownloaderPlugin

